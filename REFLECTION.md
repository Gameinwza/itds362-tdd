# A7 Retrospective

## 1. จำนวนเทสต์และอัตราส่วน

มีเทสต์ทั้งหมด 8 ตัว และโค้ดจริงใน `kitchen.py` 38 บรรทัดเมื่อไม่นับบรรทัดว่าง จึงมีอัตราส่วนเทสต์ต่อโค้ดจริงเป็น 8:38 หรือประมาณ 1:4.75 ตัวเลขนี้ไม่ได้บอกคุณภาพทั้งหมด แต่แสดงว่าพฤติกรรมหลักแต่ละข้อมีเทสต์รองรับ

## 2. ช่วง RED ที่นานที่สุด

ช่วง A3 เรื่องการคูณแล้วคืนอ็อบเจ็กต์ใหม่ใช้เวลาคิดมากที่สุด เพราะการแก้ `times` จากการเปลี่ยนค่าเดิมเป็นการคืนค่าใหม่ทำให้เทสต์ A1 และ A2 ซึ่งผูกกับพฤติกรรมเดิมต้องปรับตาม จุดนี้ทำให้เห็นว่าเทสต์เก่าก็อาจเขียนยึดติดกับ implementation ที่ผิดได้

## 3. วิธีที่ใช้ทำให้เทสต์เป็นสีเขียว

- **Fake It:** A1 ให้ `times` กำหนดค่าเป็น 600 โดยตรง เหมาะสมสำหรับยืนยันว่าการเชื่อมต่อระหว่างไฟล์เทสต์กับโค้ดทำงานจริง แต่ต้องรีบกำจัดค่าคงที่ในรอบถัดไป
- **Triangulate:** A2 เพิ่มกรณีคูณ 2 เพื่อบังคับให้เปลี่ยนจาก 600 เป็นสูตร `amount * multiplier` เหมาะสม เพราะตัวอย่างที่สองเปิดเผยกฎทั่วไปโดยไม่ต้องคาดเดา
- **Obvious Implementation:** A8 ให้ `Sum.times` คูณสมาชิกซ้ายและขวาแล้วสร้าง `Sum` ใหม่ เหมาะสม เพราะกฎสั้น ชัดเจน และมีเทสต์ของการลดรูปครอบคลุมผลลัพธ์

## 4. รายการทดสอบที่ค้างอยู่

รายการที่กำหนดมาครบทั้งหมดแล้ว จึงไม่มีรายการที่ต้องทำต่อ ส่วนการทำให้ `Quantity` hashable ถูกตัดออก เพราะยังไม่มีพฤติกรรมใดต้องใช้เป็น key ของ dictionary หรือสมาชิกของ set การเพิ่มตอนนี้จะเป็นการคาดเดาล่วงหน้า

## 5. Quantity.reduce

ถ้าออกแบบทั้งหมดบนกระดาษก่อน น่าจะให้ `Converter` ตรวจชนิดของทุก expression แล้วคำนวณเอง และอาจไม่คิดสร้าง `Quantity.reduce` ขึ้นมา เมท็อดนี้เกิดจากการค่อย ๆ แยกความรับผิดชอบ: `Converter` รู้เรื่องอัตราแปลง ส่วน `Quantity` และ `Sum` รู้วิธีลดรูปของตัวเอง ผลคือเพิ่ม expression ชนิดใหม่ได้โดยไม่ต้องเพิ่มเงื่อนไขตรวจชนิดใน `Converter`
